{
  "name": "chanter-mcp-server",
  "version": "0.6.0",
  "description": "CHANTER MCP Server – Max Edition. Safety-first control layer for CHANTER products. Checkpoint P4: AutoPoster Runtime Control.",
  "type": "module",
  "main": "dist/src/index.js",
  "bin": {
    "chanter-mcp": "dist/src/index.js"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test tests/**/*.test.ts",
    "build": "tsc",
    "start": "node dist/src/index.js",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "chanter",
    "mcp",
    "control-layer",
    "safety"
  ],
  "author": "CHANTER",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.22.0",
    "chanter-agent-runtime": "file:../chanter-agent-runtime"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
